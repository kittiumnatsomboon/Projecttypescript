import { Outlet } from "react-router-dom";

export default function Navbar(){
    return(
        <>
        Navbar<div className="bg-sky-500">
sdd
        </div>
        <Outlet/>
        </>
    )
}